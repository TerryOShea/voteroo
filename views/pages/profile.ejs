<!DOCTYPE html>
<html>
    <head><% include ../partials/head %></head>
    <body>
        <header><% include ../partials/header %></header>

        <div class='profile-box'>
            <h1 class='title'>Welcome, <%= user.local.firstname %>!</h1>
            <div class='subtitle'>My polls:</div>
            <% for (var i = 0; i < polls.length; i++) { %>
                <form action='/see_poll' method='GET'>
                    <button type='submit' name='title' value='<%= polls[i].title %>' class='poll-btn'><%= polls[i].title %></button>
                </form>
                <form action='/delete_poll' method='GET'>
                    <button type='submit' name='title' value='<%= polls[i].title %>'>Delete</button>
                </form>
            <% } %>
            <form action='/add_new_poll' method='GET'>
                <input type='submit' value='+ Add a Poll +'>
            </form>
        </div>
        
        <form action='/signout' method='GET'>
            <button title='Sign out'>Sign out</button>
        </form>
        
        <footer><% include ../partials/footer %></footer>
    </body>
</html>