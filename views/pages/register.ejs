<!DOCTYPE html>
<html>
    <head><% include ../partials/head %></head>
    <body>
        <header><% include ../partials/header %></header>

        <% if (message.length > 0) { %>
            <div class='alert'><%= message %></div>
        <% } %>

        <div class='si-reg-container'>
            <h1 class='title'>Register</h1>
            <h4 class='subtitle'>( Already have an account? <a href='/signin'>Sign in here →</a> )</h4>
            <form action='/auth/twitter' method='GET'>
                <button type="submit" title="Register through Twitter" class="si-reg-btn si-reg-tw-btn">
                    Register through Twitter <i class="fa fa-lg fa-twitter"></i>
                </button>
            </form>
            <form action='/auth/facebook' method='GET'>
                <button type="submit" title="Register through Facebook" class="si-reg-btn si-reg-fb-btn">
                    Register through Facebook <i class="fa fa-lg fa-facebook"></i>
                </button>
            </form>
            <hr>or</hr>

            <form class='local-si-reg-form' action='/register' method='POST'>
                <input type='text' class='si-reg-input' name='firstname' placeholder='First name'>
                <input type='text' class='si-reg-input' name='lastname' placeholder='Last name'>
                <input type='email' class='si-reg-input' name='email' placeholder='Email address'>
                <input type='password' class='si-reg-input' name='password' placeholder='Password'>
                <button type='submit' class='si-reg-btn local-si-reg-btn'>Register</button>
            </form>
        </div>
        <footer><% include ../partials/footer %></footer>
    </body>
</html>